================================================================================
REGISTRO DE ALTERACOES — RBAC CAv4 (FRONT-END)
Referencia: backend/docs/mapeamento_rbac_cav4.md
================================================================================

SESSAO 1 — Guards de rota por permissao granular
--------------------------------------------------------------------------------

1. components/auth/protected-route.tsx
   - Nova prop opcional: requiredPermissions?: Permission[]
   - Logica: checkAnyPermission(user.permissions, requiredPermissions) como
     criterio primario; allowedUserTypes como fallback para sessoes sem o
     campo permissions (compatibilidade retroativa)
   - Import adicionado: checkAnyPermission, Permission (lib/auth/permissions)

2. app/compartilhamentos/page.tsx
   - ProtectedRoute recebe requiredPermissions={["shares:read"]}
   - allowedUserTypes mantido: ["internal", "supervisor"]

3. app/historico/page.tsx
   - ProtectedRoute recebe requiredPermissions={["shares:read"]}
   - allowedUserTypes mantido: ["internal", "supervisor"]

4. app/upload/page.tsx
   - ProtectedRoute recebe requiredPermissions={["file:upload", "shares:create"]}
   - useEffect guard inline redundante removido (logica centralizada no ProtectedRoute)
   - Import removido: checkAnyPermission (nao mais utilizado diretamente)

5. app/logs/page.tsx
   - Import adicionado: checkAnyPermission (lib/auth/permissions)
   - useEffect guard atualizado: checkAnyPermission(["report:read"]) ||
     userType === supervisor/admin
   - Render guard atualizado com mesma logica

6. app/auditoria/page.tsx
   - Import adicionado: checkAnyPermission (lib/auth/permissions)
   - useEffect guard adicionado: checkAnyPermission(["audit:read"]) ||
     userType === admin/supervisor
   - Render guard adicionado com mesma logica

7. app/download/page.tsx
   - Import adicionado: checkAnyPermission (lib/auth/permissions)
   - useEffect guard principal atualizado: checkAnyPermission(["file:download"]) ||
     userType === external
   - useEffect de fetch atualizado com mesma logica

8. app/supervisor/page.tsx
   - Render guard atualizado: checkAnyPermission(["shares:approve","shares:reject"]) ||
     userType === supervisor


SESSAO 2 — Menu de navegacao dinamico por permissao
--------------------------------------------------------------------------------

9. lib/auth/nav-items.ts  [ARQUIVO NOVO]
   - Define ALL_NAV_ITEMS: array com os 7 modulos do sistema, cada um com:
     * label, route, icon (Lucide)
     * requiredPermissions: Permission[]  (criterio primario — CAv4)
     * requiredUserTypes: string[]        (fallback para sessoes antigas)
   - Modulos definidos:
     * Upload            -> file:upload | shares:create    | internal, supervisor
     * Compartilhamentos -> shares:read                    | internal, supervisor
     * Historico         -> shares:read                    | internal, supervisor
     * Painel do Gestor  -> shares:approve | shares:reject | supervisor
     * Logs              -> report:read                    | supervisor, admin
     * Auditoria         -> audit:read                     | admin, supervisor
     * Administracao     -> *                              | admin
   - Exporta getNavItems(user): filtra e retorna so os itens liberados para o usuario

10. components/shared/app-header.tsx
    - Imports removidos: FolderOpen, Activity, usePermissions
    - Imports adicionados: usePathname, getNavItems, cn
    - Constantes removidas: canViewCompartilhamentos, canViewLogs (logica migrada)
    - Adicionado: pathname = usePathname(), navItems = getNavItems(user)
    - Handler adicionado: handleNavigate(route) — unifica navegacao + fechamento do Sheet
    - Dropdown do avatar (desktop):
      * Secao fixa "Meus Compartilhamentos" e "Logs" substituida pela lista dinamica
      * Titulo de secao "Navegacao" exibido quando navItems.length > 0
      * Item ativo destacado (bg-blue-50 / blue-900/20 + ponto indicador)
    - Sheet mobile:
      * Botoes fixos substituidos pelo map de navItems com mesmo destaque de ativo
      * Separador e botao "Sair" mantidos


================================================================================
ARQUIVOS NAO ALTERADOS (confirmados intactos)
================================================================================

- lib/auth/permissions.ts          (sem alteracao)
- lib/stores/auth-store.ts         (sem alteracao)
- app/admin/page.tsx               (sem alteracao)
- app/supervisor/detalhes/[id]/page.tsx  (sem alteracao)
- app/layout.tsx                   (sem alteracao)
- backend/*                        (sem alteracao — nenhuma regra de negocio alterada)


================================================================================
COMPORTAMENTO ESPERADO POS-ALTERACAO
================================================================================

- O menu no dropdown da foto exibe somente os modulos que o usuario tem acesso.
- Alterar permissions no CAv4 (backend) reflete automaticamente no menu na proxima
  renderizacao sem necessidade de alteracao de codigo frontend.
- Sessoes antigas (sem campo permissions) continuam funcionando via fallback userType.
- Nenhuma regra de negocio ou logica de backend foi modificada.
================================================================================
