version: "3.9"

services:
  api:
    build: .
    container_name: backend
    ports:
      - "8000:8000"
    volumes:
      - .:/app
    environment:
      UVICORN_RELOAD: "true"
    command: uvicorn app.main:app --host 0.0.0.0 --port 8000 --reload
